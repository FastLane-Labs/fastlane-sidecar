name: Build and Publish DEB Package

on:
  push:
    branches: [ main ]
    tags: [ '*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-deb:
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y dpkg-dev libsystemd-dev

    - name: Read version
      id: version
      run: |
        VERSION=$(cat VERSION)
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

    - name: Determine repository
      id: repo
      run: |
        if [[ "${{ github.ref }}" == refs/tags/* ]]; then
          echo "BUCKET=fastlane-apt-repo" >> $GITHUB_OUTPUT
          echo "DOMAIN=pkg.fastlane.xyz" >> $GITHUB_OUTPUT
        else
          echo "BUCKET=fastlane-apt-repo-stage" >> $GITHUB_OUTPUT
          echo "DOMAIN=pkg-stage.fastlane.xyz" >> $GITHUB_OUTPUT
        fi

    - name: Build DEB package
      run: |
        ./debian/build-deb.sh ${{ steps.version.outputs.VERSION }}

    - name: Upload DEB package as artifact
      uses: actions/upload-artifact@v4
      with:
        name: fastlane-sidecar-deb
        path: build/*.deb

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-central-1

    - name: Install APT repository tools
      run: |
        sudo apt-get install -y dpkg-dev gpg

    - name: Create APT repository structure
      run: |
        # Create repository directory
        mkdir -p apt-repo/pool/main
        mkdir -p apt-repo/dists/noble/main/binary-amd64

        cp build/*.deb apt-repo/pool/main/
        aws s3 cp build/*.deb s3://${{ steps.repo.outputs.BUCKET }}/pool/main/

    - name: Generate repository metadata
      run: |
        cd apt-repo

        # Download existing Packages file (small file, not the actual packages)
        aws s3 cp s3://${{ steps.repo.outputs.BUCKET }}/dists/noble/main/binary-amd64/Packages existing_packages.txt || touch existing_packages.txt

        # Generate metadata for new package only
        dpkg-scanpackages pool/main /dev/null > new_packages.txt

        # Combine existing and new package metadata (remove duplicates)
        cat existing_packages.txt new_packages.txt | sort | uniq > dists/noble/main/binary-amd64/Packages

        # Compress the Packages file
        gzip -c dists/noble/main/binary-amd64/Packages > dists/noble/main/binary-amd64/Packages.gz

        # Create Release file
        cat > dists/noble/Release << EOF
        Origin: Fastlane Labs
        Label: FastLane APT Repository
        Suite: noble
        Codename: noble
        Date: $(date -Ru)
        Architectures: amd64
        Components: main
        Description: FastLane APT Repository
        EOF

        # Add checksums to Release file
        echo "MD5Sum:" >> dists/noble/Release
        find dists/noble -name "Packages*" -type f -exec md5sum {} \; | sed 's/dists\/noble\///g' | awk '{print " " $1 " " $2}' >> dists/noble/Release

        echo "SHA1:" >> dists/noble/Release
        find dists/noble -name "Packages*" -type f -exec sha1sum {} \; | sed 's/dists\/noble\///g' | awk '{print " " $1 " " $2}' >> dists/noble/Release

        echo "SHA256:" >> dists/noble/Release
        find dists/noble -name "Packages*" -type f -exec sha256sum {} \; | sed 's/dists\/noble\///g' | awk '{print " " $1 " " $2}' >> dists/noble/Release

        # Create repository list file
        cat > fastlane-labs.list << EOF
        # FastLane APT Repository
        deb https://${{ steps.repo.outputs.DOMAIN }}/ noble main
        EOF

    - name: Sync to S3 bucket
      run: |
        cd apt-repo

        # Sync only metadata files to S3 (packages already uploaded)
        aws s3 sync dists/ s3://${{ steps.repo.outputs.BUCKET }}/dists/
        aws s3 cp fastlane-labs.list s3://${{ steps.repo.outputs.BUCKET }}/fastlane-labs.list

        # Set proper content types
        aws s3 cp fastlane-labs.list s3://${{ steps.repo.outputs.BUCKET }}/fastlane-labs.list --content-type "text/plain"
        aws s3 cp dists/noble/Release s3://${{ steps.repo.outputs.BUCKET }}/dists/noble/Release --content-type "text/plain"
        aws s3 cp dists/noble/main/binary-amd64/Packages s3://${{ steps.repo.outputs.BUCKET }}/dists/noble/main/binary-amd64/Packages --content-type "text/plain"

    - name: Verify repository upload
      run: |
        echo "Repository files uploaded to S3:"
        aws s3 ls s3://${{ steps.repo.outputs.BUCKET }}/ --recursive

